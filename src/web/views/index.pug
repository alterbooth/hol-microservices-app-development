extends layout

block content
  section.section
    div.container
      h1#title.title.has-text-info= title
      p#message #{message}
  section.section
    div.container
      p #{request}
  section.section
    div.container
      p #{response}